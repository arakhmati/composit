from setuptools import setup
from setuptools import find_packages

setup(
    name="networkx_persistent",
    version="0.0.0",
    description="Persistent Implementation of NetworkX.",
    packages=find_packages(),
)
